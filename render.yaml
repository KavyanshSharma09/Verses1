services:
  - type: web
    name: verses1
    runtime: python
    plan: free
    rootDir: verses1
    buildCommand: bash ./build.sh
    startCommand: gunicorn verses1.wsgi:application
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: DATABASE_URL
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.0"
